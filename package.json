{
  "name": "esbuild-plugin-inline-css",
  "version": "0.0.1",
  "description": "Inline CSS Loader for esbuild",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Evan Reichard",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^10.0.0",
    "path": "^0.12.7",
    "sha256": "^0.2.0"
  }
}
